# Time Limit Exceeded Error(TLE)

"""
Time Limit -> It takes around 1 Second to execute 10^8 Operations(If,If..else,For Loop,While Loop,etc. Consider as One Operation)
            - When it Exceed the Operations limit it also exceed the time so it Give TLE.
"""
